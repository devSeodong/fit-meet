spring:
  application:
    name: FitMeet

  thymeleaf:
    mode: HTML
    suffix: .html
    prefix: classpath:templates/
    check-template-location: true

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/fitmeet?serverTimezone=Asia/Seoul&useUnicode=true&characterEncoding=UTF-8
    username: seodong
    password: Tjgud990517!

  web:
    resources:
      static-locations: /static/

mybatis:
  type-aliases-package: com.ssafy.fitmeet.domain.*.entity
  mapper-locations: classpath:mappers/**/*.xml
  configuration:
    map-underscore-to-camel-case: true
    cache-enabled: false
    jdbc-type-for-null: 'NULL'

springdoc:
  swagger-ui:
    path: /swagger-ui.html
  api-docs:
    path: /v3/api-docs

server:
  port: 9090

jwt:
  secret: "fitmeettsttestesttest1235test54345346testestestestestest"
  access-token-validity-in-ms: 3600000

logging:
  level:
    org.mybatis: DEBUG
    org.mybatis.spring: DEBUG
    com.ssafy.fitmeet: DEBUG

